<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Admin.OverduePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Kiểm tra Quá hạn">
    <Grid RowDefinitions="Auto,*">
        <Frame Grid.Row="0" 
               Padding="15" 
               BackgroundColor="#FF2196F3"
               HasShadow="True"
               CornerRadius="0">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                <Button Grid.Column="0" 
                        Text="← Quay lại" 
                        Clicked="OnBackClicked"
                        BackgroundColor="#FF1976D2"
                        TextColor="White"
                        CornerRadius="8"
                        WidthRequest="90"/>
                <Label Grid.Column="1" 
                       Text="⏰ Kiểm tra Quá hạn" 
                       FontSize="22" 
                       FontAttributes="Bold"
                       TextColor="White"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>
            </Grid>
        </Frame>
        <Grid Grid.Row="1" RowDefinitions="Auto,Auto,Auto,Auto,*">
            <Frame Grid.Row="0"
                   Padding="20"
                   BackgroundColor="White"
                   BorderColor="#FFE0E0E0"
                   CornerRadius="0"
                   HasShadow="False">
                <VerticalStackLayout Spacing="15">
                    <Label Text="📋 Danh sách tất cả sách và người dùng đang mượn sách" 
                           FontSize="15" 
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="#FF424242"
                           Margin="0,5"/>
                
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                        <Button Grid.Column="0"
                                Text="🔔 Kiểm tra và Thông báo" 
                                Clicked="OnCheckOverdueClicked"
                                BackgroundColor="#FFFF9800"
                                TextColor="White"
                                CornerRadius="10"
                                FontAttributes="Bold"
                                FontSize="14"
                                HeightRequest="45"/>
                        <Button Grid.Column="1"
                                Text="🔄 Làm mới" 
                                Clicked="OnViewOverdueListClicked"
                                BackgroundColor="#FF2196F3"
                                TextColor="White"
                                CornerRadius="10"
                                FontAttributes="Bold"
                                FontSize="14"
                                HeightRequest="45"/>
                    </Grid>
                    
                    <Frame Padding="12"
                           BackgroundColor="#FFF5F5F5"
                           BorderColor="#FFE0E0E0"
                           CornerRadius="10"
                           HasShadow="False"
                           IsVisible="False">
                        <Label x:Name="CountLabel" 
                               Text="Danh sách sách đang mượn" 
                               FontSize="15" 
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               TextColor="#FF424242"/>
                    </Frame>

                    <ActivityIndicator x:Name="LoadingIndicator" 
                                      IsRunning="False" 
                                      IsVisible="False" 
                                      Color="#FF2196F3"
                                      Margin="0,5"/>

                    <Frame Padding="12"
                           BackgroundColor="#FFF5F5F5"
                           BorderColor="#FFE0E0E0"
                           CornerRadius="10"
                           HasShadow="False"
                           IsVisible="False">
                        <Label x:Name="ResultLabel" 
                               FontSize="14" 
                               TextColor="#FF424242"
                               HorizontalOptions="Center"/>
                    </Frame>
                </VerticalStackLayout>
            </Frame>

            <Label x:Name="EmptyLabel" 
                   Grid.Row="4"
                   Text="Không có sách nào đang được mượn" 
                   FontSize="16"
                   IsVisible="False"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"/>

            <CollectionView x:Name="OverdueCollection" 
                           Grid.Row="4"
                           IsVisible="False">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="15,10" 
                               Padding="20"
                               BackgroundColor="White"
                               BorderColor="#FFE0E0E0"
                               CornerRadius="15"
                               HasShadow="True">
                            <VerticalStackLayout Spacing="12">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                    <Label Grid.Column="0"
                                           Text="{Binding ReaderName}" 
                                           FontSize="20" 
                                           FontAttributes="Bold"
                                           TextColor="#FF2196F3"/>
                                    <Frame Grid.Column="1"
                                           Padding="10,5"
                                           BackgroundColor="#FFF5F5F5"
                                           BorderColor="#FFE0E0E0"
                                           CornerRadius="8"
                                           HasShadow="False">
                                        <Label Text="{Binding ReaderCardCode, StringFormat='🪪 {0}'}" 
                                               FontSize="12"
                                               FontAttributes="Bold"
                                               TextColor="#FF757575"/>
                                    </Frame>
                                </Grid>
                                
                                <BoxView HeightRequest="1" Color="#FFE0E0E0" Margin="0,5"/>
                                
                                <Label Text="{Binding BookTitle}" 
                                       FontSize="18" 
                                       FontAttributes="Bold"
                                       TextColor="#FF212121"/>
                                <Label Text="{Binding BookManagementCode, StringFormat='🏷️ Mã sách: {0}'}" 
                                       FontSize="13"
                                       TextColor="#FF757575"/>
                                
                                <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,10">
                                    <VerticalStackLayout Grid.Column="0" Spacing="5">
                                        <Label Text="{Binding BorrowDate, StringFormat='📅 Mượn: {0:yyyy-MM-dd}'}" 
                                               FontSize="13"
                                               TextColor="#FF757575"/>
                                        <Label Text="{Binding DueDate, StringFormat='⏰ Hạn: {0:yyyy-MM-dd}'}" 
                                               FontSize="13"
                                               FontAttributes="Bold"
                                               TextColor="#FFFF5722"/>
                                    </VerticalStackLayout>
                                    <VerticalStackLayout Grid.Column="1" Spacing="5">
                                        <Label Text="{Binding DaysOverdue, StringFormat='⚠️ Quá hạn: {0} ngày'}" 
                                               FontSize="13"
                                               FontAttributes="Bold"
                                               TextColor="#FFF44336"
                                               IsVisible="{Binding DaysOverdue, Converter={StaticResource IsGreaterThanZeroConverter}}"/>
                                        <Label Text="{Binding DaysRemaining, StringFormat='✅ Còn lại: {0} ngày'}" 
                                               FontSize="13"
                                               FontAttributes="Bold"
                                               TextColor="#FF4CAF50"
                                               IsVisible="{Binding DaysOverdue, Converter={StaticResource IsZeroOrLessConverter}}"/>
                                    </VerticalStackLayout>
                                </Grid>
                                
                                <BoxView HeightRequest="1" Color="#FFE0E0E0" Margin="0,10"/>
                                
                                <VerticalStackLayout Spacing="6">
                                    <Label Text="{Binding ReaderEmail, StringFormat='📧 {0}'}" 
                                           FontSize="13"
                                           TextColor="#FF757575"/>
                                    <Label Text="{Binding ReaderPhone, StringFormat='📱 {0}'}" 
                                           FontSize="13"
                                           TextColor="#FF757575"
                                           IsVisible="{Binding ReaderPhone, Converter={StaticResource StringToBoolConverter}}"/>
                                    <Label Text="{Binding ReaderAddress, StringFormat='📍 {0}'}" 
                                           FontSize="13"
                                           TextColor="#FF757575"
                                           LineBreakMode="WordWrap"
                                           IsVisible="{Binding ReaderAddress, Converter={StaticResource StringToBoolConverter}}"/>
                                </VerticalStackLayout>
                                
                                <Button Text="📧 Gửi Email Quá Hạn" 
                                        Clicked="OnSendEmailClicked"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#FF4CAF50"
                                        TextColor="White"
                                        CornerRadius="10"
                                        FontAttributes="Bold"
                                        FontSize="14"
                                        HeightRequest="45"
                                        Margin="0,15,0,0"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>

