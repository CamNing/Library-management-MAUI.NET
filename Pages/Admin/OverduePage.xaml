<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Admin.OverduePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Kiểm tra Quá hạn"
             BackgroundColor="#F3F4F6">

    <Grid>
        <BoxView InputTransparent="True">
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#F3F4F6" Offset="0.0" />
                    <GradientStop Color="#EDE9FE" Offset="1.0" />
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>

        <Grid RowDefinitions="Auto, Auto, *">

            <VerticalStackLayout Grid.Row="0" Spacing="15" Padding="20,10,20,10">
                <Grid ColumnDefinitions="Auto, *">
                    <Border Grid.Column="0" 
                            StrokeShape="RoundRectangle 12" 
                            BackgroundColor="White" 
                            StrokeThickness="0"
                            HeightRequest="40" 
                            WidthRequest="40"
                            VerticalOptions="Center">
                        <Border.Shadow>
                            <Shadow Brush="#000000" Opacity="0.05" Offset="0,4" Radius="10" />
                        </Border.Shadow>
                        <Label Text="➜" 
       Rotation="180"
       
       FontSize="20" 
       TextColor="#1F2937" 
       HorizontalOptions="Center" 
       VerticalOptions="Center"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnBackClicked"/>
                        </Border.GestureRecognizers>
                    </Border>

                    <VerticalStackLayout Grid.Column="1" Spacing="0" VerticalOptions="Center" Margin="15,0,0,0">
                        <Label Text="Quản trị viên" FontSize="12" TextColor="#6B7280" />
                        <Label Text="Kiểm Tra Quá Hạn" FontSize="20" FontAttributes="Bold" TextColor="#1F2937" />
                    </VerticalStackLayout>
                </Grid>
            </VerticalStackLayout>

            <Border Grid.Row="1"
                    Margin="20,0,20,15"
                    Padding="20"
                    BackgroundColor="White"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 15">
                <Border.Shadow>
                    <Shadow Brush="#000000" Opacity="0.05" Offset="0,4" Radius="10"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="15">
                    <Label Text="📋 Thao tác quản lý" 
                           FontSize="14" 
                           FontAttributes="Bold" 
                           TextColor="#6C63FF"/>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                        <Button Grid.Column="0"
                                Text="🔔 Kiểm tra &amp; TB" 
                                Clicked="OnCheckOverdueClicked"
                                BackgroundColor="#F97316"
                                TextColor="White"
                                CornerRadius="12"
                                FontAttributes="Bold"
                                HeightRequest="50">
                            <Button.Shadow>
                                <Shadow Brush="#F97316" Offset="0,4" Opacity="0.4" Radius="8"/>
                            </Button.Shadow>
                        </Button>

                        <Button Grid.Column="1"
                                Text="🔄 Làm mới" 
                                Clicked="OnViewOverdueListClicked"
                                BackgroundColor="#6C63FF"
                                TextColor="White"
                                CornerRadius="12"
                                FontAttributes="Bold"
                                HeightRequest="50">
                            <Button.Shadow>
                                <Shadow Brush="#6C63FF" Offset="0,4" Opacity="0.4" Radius="8"/>
                            </Button.Shadow>
                        </Button>
                    </Grid>

                    <VerticalStackLayout Spacing="5">
                        <Border BackgroundColor="#F3F4F6" StrokeShape="RoundRectangle 8" Padding="10" StrokeThickness="0"
                                IsVisible="{Binding IsVisible, Source={x:Reference CountLabel}}">
                            <Label x:Name="CountLabel" 
                                   Text="Danh sách sách đang mượn" 
                                   FontSize="13" 
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center" 
                                   TextColor="#4B5563"/>
                        </Border>

                        <ActivityIndicator x:Name="LoadingIndicator" 
                                           IsRunning="False" 
                                           IsVisible="False" 
                                           Color="#6C63FF"
                                           Margin="0,5"/>

                        <Border BackgroundColor="#ECFDF5" StrokeShape="RoundRectangle 8" Padding="10" StrokeThickness="0"
                                IsVisible="{Binding IsVisible, Source={x:Reference ResultLabel}}">
                            <Label x:Name="ResultLabel" 
                                   FontSize="13" 
                                   TextColor="#059669" 
                                   HorizontalOptions="Center"/>
                        </Border>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Grid Grid.Row="2">
                <Label x:Name="EmptyLabel" 
                       Text="✨ Không có sách nào đang được mượn" 
                       FontSize="16"
                       TextColor="#9CA3AF"
                       IsVisible="False"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>

                <CollectionView x:Name="OverdueCollection" IsVisible="False">
                    <CollectionView.Header>
                        <BoxView HeightRequest="5" Color="Transparent"/>
                    </CollectionView.Header>
                    <CollectionView.Footer>
                        <BoxView HeightRequest="20" Color="Transparent"/>
                    </CollectionView.Footer>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="20,0,20,15" 
                                    Padding="20"
                                    BackgroundColor="White"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 15">
                                <Border.Shadow>
                                    <Shadow Brush="#000000" Opacity="0.05" Offset="0,4" Radius="10"/>
                                </Border.Shadow>

                                <VerticalStackLayout Spacing="10">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label Grid.Column="0"
                                               Text="{Binding ReaderName}" 
                                               FontSize="16" 
                                               FontAttributes="Bold"
                                               TextColor="#6C63FF"
                                               VerticalOptions="Center"/>

                                        <Border Grid.Column="1"
                                                BackgroundColor="#F3F4F6"
                                                StrokeThickness="0"
                                                StrokeShape="RoundRectangle 8"
                                                Padding="10,5">
                                            <Label Text="{Binding ReaderCardCode, StringFormat='🪪 {0}'}" 
                                                   FontSize="11" 
                                                   FontAttributes="Bold" 
                                                   TextColor="#6B7280"/>
                                        </Border>
                                    </Grid>

                                    <BoxView HeightRequest="1" Color="#F3F4F6"/>

                                    <VerticalStackLayout Spacing="2">
                                        <Label Text="{Binding BookTitle}" 
                                               FontSize="15" 
                                               FontAttributes="Bold"
                                               TextColor="#1F2937"/>
                                        <Label Text="{Binding BookManagementCode, StringFormat='🏷️ Mã sách: {0}'}" 
                                               FontSize="12"
                                               TextColor="#9CA3AF"/>
                                    </VerticalStackLayout>

                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,5">
                                        <VerticalStackLayout Grid.Column="0" Spacing="2">
                                            <Label Text="{Binding BorrowDate, StringFormat='Ngày mượn: {0:yyyy-MM-dd}'}" FontSize="12" TextColor="#6B7280"/>
                                            <Label Text="{Binding DueDate, StringFormat='Hạn trả: {0:yyyy-MM-dd}'}" FontSize="13" TextColor="#EF4444" FontAttributes="Bold"/>
                                        </VerticalStackLayout>

                                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                            <Border BackgroundColor="#FEF2F2" StrokeShape="RoundRectangle 8" StrokeThickness="0" Padding="10"
                                                    IsVisible="{Binding DaysOverdue, Converter={StaticResource IsGreaterThanZeroConverter}}">
                                                <Label Text="{Binding DaysOverdue, StringFormat='⚠️ Quá hạn: {0} ngày'}" 
                                                       FontSize="13" FontAttributes="Bold" TextColor="#EF4444" HorizontalOptions="Center"/>
                                            </Border>

                                            <Border BackgroundColor="#ECFDF5" StrokeShape="RoundRectangle 8" StrokeThickness="0" Padding="10"
                                                    IsVisible="{Binding DaysOverdue, Converter={StaticResource IsZeroOrLessConverter}}">
                                                <Label Text="{Binding DaysRemaining, StringFormat='✅ Còn lại: {0} ngày'}" 
                                                       FontSize="13" FontAttributes="Bold" TextColor="#10B981" HorizontalOptions="Center"/>
                                            </Border>
                                        </VerticalStackLayout>
                                    </Grid>

                                    <BoxView HeightRequest="1" Color="#F3F4F6"/>

                                    <VerticalStackLayout Spacing="4">
                                        <Label Text="{Binding ReaderEmail, StringFormat='📧 {0}'}" FontSize="12" TextColor="#6B7280"/>
                                        <Label Text="{Binding ReaderPhone, StringFormat='📱 {0}'}" FontSize="12" TextColor="#6B7280" 
                                               IsVisible="{Binding ReaderPhone, Converter={StaticResource StringToBoolConverter}}"/>
                                        <Label Text="{Binding ReaderAddress, StringFormat='📍 {0}'}" FontSize="12" TextColor="#6B7280" LineBreakMode="WordWrap" 
                                               IsVisible="{Binding ReaderAddress, Converter={StaticResource StringToBoolConverter}}"/>
                                    </VerticalStackLayout>

                                    <Button Text="📧 Gửi Email Quá Hạn" 
                                            Clicked="OnSendEmailClicked"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="#6C63FF"
                                            TextColor="White"
                                            CornerRadius="12"
                                            FontAttributes="Bold"
                                            HeightRequest="45"
                                            Margin="0,10,0,0">
                                        <Button.Shadow>
                                            <Shadow Brush="#6C63FF" Offset="0,3" Opacity="0.3" Radius="6"/>
                                        </Button.Shadow>
                                    </Button>

                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>