<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Admin.BorrowRequestsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Yêu cầu Mượn">
    <Grid RowDefinitions="Auto,Auto,*">
        <Frame Grid.Row="0" 
               Padding="15" 
               BackgroundColor="#FF2196F3"
               HasShadow="True"
               CornerRadius="0">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                <Button Grid.Column="0" 
                        Text="← Quay lại" 
                        Clicked="OnBackClicked"
                        BackgroundColor="#FF1976D2"
                        TextColor="White"
                        CornerRadius="8"
                        WidthRequest="90"/>
                <Label Grid.Column="1" 
                       Text="📋 Yêu cầu Mượn" 
                       FontSize="22" 
                       FontAttributes="Bold"
                       TextColor="White"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>
            </Grid>
        </Frame>
        <Frame Grid.Row="1"
               Padding="15"
               BackgroundColor="White"
               BorderColor="#FFE0E0E0"
               CornerRadius="0"
               HasShadow="False">
            <ScrollView Orientation="Horizontal">
                <HorizontalStackLayout Spacing="10">
                    <Button x:Name="FilterAllButton" 
                            Text="📑 Tất cả" 
                            Clicked="OnFilterAllClicked"
                            BackgroundColor="#FF2196F3"
                            TextColor="White"
                            CornerRadius="10"
                            FontAttributes="Bold"
                            MinimumWidthRequest="90"
                            HeightRequest="40"/>
                    <Button x:Name="FilterPendingButton" 
                            Text="⏳ Chờ duyệt" 
                            Clicked="OnFilterPendingClicked"
                            BackgroundColor="#FF9E9E9E"
                            TextColor="White"
                            CornerRadius="10"
                            FontAttributes="Bold"
                            MinimumWidthRequest="100"
                            HeightRequest="40"/>
                    <Button x:Name="FilterApprovedButton" 
                            Text="✅ Đã duyệt" 
                            Clicked="OnFilterApprovedClicked"
                            BackgroundColor="#FF9E9E9E"
                            TextColor="White"
                            CornerRadius="10"
                            FontAttributes="Bold"
                            MinimumWidthRequest="100"
                            HeightRequest="40"/>
                    <Button x:Name="FilterRejectedButton" 
                            Text="❌ Đã từ chối" 
                            Clicked="OnFilterRejectedClicked"
                            BackgroundColor="#FF9E9E9E"
                            TextColor="White"
                            CornerRadius="10"
                            FontAttributes="Bold"
                            MinimumWidthRequest="110"
                            HeightRequest="40"/>
                    <Button Text="🔄 Làm mới" 
                            Clicked="OnRefreshClicked"
                            BackgroundColor="#FF4CAF50"
                            TextColor="White"
                            CornerRadius="10"
                            FontAttributes="Bold"
                            MinimumWidthRequest="100"
                            HeightRequest="40"/>
                </HorizontalStackLayout>
            </ScrollView>
        </Frame>
        <CollectionView x:Name="RequestsCollection" 
                       Grid.Row="2"
                       ItemsSource="{Binding Requests}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="15,10" 
                           Padding="20"
                           BackgroundColor="White"
                           BorderColor="#FFE0E0E0"
                           CornerRadius="15"
                           HasShadow="True">
                        <VerticalStackLayout Spacing="12">
                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                <VerticalStackLayout Grid.Column="0" Spacing="5">
                                    <Label Text="{Binding ReaderName, StringFormat='👤 {0}'}" 
                                           FontSize="18" 
                                           FontAttributes="Bold"
                                           TextColor="#FF212121"/>
                                    <Label Text="{Binding ReaderEmail, StringFormat='📧 {0}'}" 
                                           FontSize="14"
                                           TextColor="#FF757575"/>
                                </VerticalStackLayout>
                                <Frame Grid.Column="1"
                                       Padding="10,5"
                                       BackgroundColor="#FFF5F5F5"
                                       BorderColor="#FFE0E0E0"
                                       CornerRadius="8"
                                       HasShadow="False">
                                    <Label Text="{Binding Status, StringFormat='{0}'}" 
                                           FontSize="13" 
                                           FontAttributes="Bold"
                                           TextColor="{Binding StatusColor}"/>
                                </Frame>
                            </Grid>
                            <BoxView HeightRequest="1" Color="#FFE0E0E0" Margin="0,5"/>
                            <Label Text="{Binding BookTitles}" 
                                   FontSize="15"
                                   LineBreakMode="WordWrap"
                                   TextColor="#FF424242"
                                   Margin="0,5,0,0"/>
                            <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,5">
                                <Label Grid.Column="0"
                                       Text="{Binding DueDate, StringFormat='⏰ Hạn trả: {0:dd/MM/yyyy}'}" 
                                       FontSize="13"
                                       TextColor="#FF757575"/>
                                <Label Grid.Column="1"
                                       Text="{Binding CreatedAt, StringFormat='📅 {0:dd/MM/yyyy HH:mm}'}" 
                                       FontSize="12"
                                       TextColor="#FF9E9E9E"
                                       HorizontalOptions="End"/>
                            </Grid>
                            <Label Text="{Binding RejectionReason, StringFormat='❌ Lý do: {0}'}" 
                                   FontSize="13"
                                   TextColor="#FFF44336"
                                   IsVisible="{Binding HasRejectionReason}"
                                   Margin="0,5,0,0"/>
                            <HorizontalStackLayout Spacing="10" Margin="0,10,0,0">
                                <Button Text="👁️ Chi tiết" 
                                        Clicked="OnViewDetailsClicked"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#FF2196F3"
                                        TextColor="White"
                                        CornerRadius="8"
                                        FontAttributes="Bold"
                                        HorizontalOptions="FillAndExpand"
                                        HeightRequest="40"/>
                                <Button Text="✅ Duyệt" 
                                        Clicked="OnApproveClicked"
                                        CommandParameter="{Binding .}"
                                        IsEnabled="{Binding CanApprove}"
                                        BackgroundColor="#FF4CAF50"
                                        TextColor="White"
                                        CornerRadius="8"
                                        FontAttributes="Bold"
                                        HorizontalOptions="FillAndExpand"
                                        HeightRequest="40"/>
                                <Button Text="❌ Từ chối" 
                                        Clicked="OnRejectClicked"
                                        CommandParameter="{Binding .}"
                                        IsEnabled="{Binding CanReject}"
                                        BackgroundColor="#FFF44336"
                                        TextColor="White"
                                        CornerRadius="8"
                                        FontAttributes="Bold"
                                        HorizontalOptions="FillAndExpand"
                                        HeightRequest="40"/>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <ActivityIndicator x:Name="LoadingIndicator" 
                          Grid.Row="2"
                          IsRunning="False" 
                          IsVisible="False" 
                          VerticalOptions="Center"
                          HorizontalOptions="Center"/>
        <Label x:Name="EmptyLabel" 
               Grid.Row="2"
               Text="Không tìm thấy yêu cầu mượn sách nào" 
               FontSize="16"
               IsVisible="False"
               VerticalOptions="Center"
               HorizontalOptions="Center"/>
    </Grid>
</ContentPage>

