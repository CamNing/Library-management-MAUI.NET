<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Admin.BooksManagementPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Quản lý Sách">
    <Grid RowDefinitions="Auto,Auto,Auto,*">
        <Frame Grid.Row="0" 
               Padding="15" 
               BackgroundColor="#FF2196F3"
               HasShadow="True"
               CornerRadius="0">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                <Button Grid.Column="0" 
                        Text="← Quay lại" 
                        Clicked="OnBackClicked"
                        BackgroundColor="#FF1976D2"
                        TextColor="White"
                        CornerRadius="8"
                        WidthRequest="90"/>
                <Label Grid.Column="1" 
                       Text="📚 Quản lý Sách" 
                       FontSize="22" 
                       FontAttributes="Bold"
                       TextColor="White"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>
                <Button Grid.Column="2" 
                        Text="➕ Thêm Sách" 
                        Clicked="OnAddBookClicked"
                        BackgroundColor="#FF4CAF50"
                        TextColor="White"
                        CornerRadius="8"
                        FontAttributes="Bold"
                        WidthRequest="110"/>
            </Grid>
        </Frame>
        <Frame Grid.Row="1" 
               Padding="15"
               BackgroundColor="White"
               BorderColor="#FFE0E0E0"
               CornerRadius="0"
               HasShadow="False">
            <SearchBar x:Name="SearchBar" 
                      Placeholder="🔍 Tìm kiếm sách..."
                      SearchCommand="{Binding SearchCommand}"
                      BackgroundColor="Transparent"/>
        </Frame>

        <CollectionView x:Name="BooksCollection" 
                       Grid.Row="3"
                       ItemsSource="{Binding Books}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="15,10" 
                           Padding="20"
                           BackgroundColor="White"
                           BorderColor="#FFE0E0E0"
                           CornerRadius="15"
                           HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="15">
                            <VerticalStackLayout Grid.Column="0" Spacing="8">
                                <Label Text="{Binding Title}" 
                                       FontSize="18" 
                                       FontAttributes="Bold"
                                       TextColor="#FF212121"/>
                                <Label Text="{Binding Authors, Converter={StaticResource AuthorsConverter}, StringFormat='✍️ {0}'}" 
                                       FontSize="14"
                                       TextColor="#FF757575"/>
                                <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,5">
                                    <Label Grid.Column="0"
                                           Text="{Binding ManagementCode, StringFormat='🏷️ {0}'}" 
                                           FontSize="12"
                                           TextColor="#FF9E9E9E"/>
                                    <Label Grid.Column="1"
                                           Text="{Binding Category, StringFormat='📂 {0}'}" 
                                           FontSize="12"
                                           TextColor="#FF9E9E9E"
                                           HorizontalOptions="End"/>
                                </Grid>
                                <Label Text="{Binding AvailableQuantity, StringFormat='📚 Có sẵn: {0} / {1}'}" 
                                       FontSize="13"
                                       FontAttributes="Bold"
                                       TextColor="{Binding AvailableQuantity, Converter={StaticResource AvailabilityColorConverter}}"/>
                            </VerticalStackLayout>
                            <Button Grid.Column="1" 
                                    Text="✏️ Sửa" 
                                    Clicked="OnEditBookClicked"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="#FFFF9800"
                                    TextColor="White"
                                    CornerRadius="8"
                                    FontAttributes="Bold"
                                    WidthRequest="85"
                                    HeightRequest="40"/>
                            <Button Grid.Column="2" 
                                    Text="🗑️ Xóa" 
                                    Clicked="OnDeleteBookClicked"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="#FFF44336"
                                    TextColor="White"
                                    CornerRadius="8"
                                    FontAttributes="Bold"
                                    WidthRequest="85"
                                    HeightRequest="40"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>

