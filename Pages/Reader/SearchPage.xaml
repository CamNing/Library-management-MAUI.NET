<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Reader.SearchPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:book.Converters"
             Title="Tìm kiếm Sách"
             BackgroundColor="#F3F4F6"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, Auto, *">

        <Grid Grid.Row="0" ColumnDefinitions="Auto, *" Padding="20,15,20,15" BackgroundColor="White">
            <Border StrokeShape="RoundRectangle 20" 
                    HeightRequest="40" 
                    WidthRequest="40" 
                    BackgroundColor="#F3F4F6" 
                    StrokeThickness="0"
                    HorizontalOptions="Start">
                <Label Text="➜" 
                       Rotation="180"
                       FontSize="20" 
                       TextColor="#1F2937" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"/>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnBackClicked"/>
                </Border.GestureRecognizers>
            </Border>

            <Label Grid.Column="1" 
                   Text="Tìm kiếm tác phẩm" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   TextColor="#1F2937"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   Margin="-40,0,0,0"/>
        </Grid>

        <Border Grid.Row="1" 
                Margin="20,20,20,10"
                StrokeShape="RoundRectangle 15"
                StrokeThickness="0"
                BackgroundColor="White"
                HeightRequest="55">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,5" Radius="15" Opacity="0.05"/>
            </Border.Shadow>

            <Grid ColumnDefinitions="50, *, 50">
                <Label Text="🔍" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#9CA3AF"/>

                <SearchBar x:Name="SearchBar" 
                           Grid.Column="1"
                           Placeholder="Nhập tên sách, tác giả..."
                           PlaceholderColor="#9CA3AF"
                           TextColor="#1F2937"
                           SearchCommand="{Binding SearchCommand}"
                           TextChanged="OnSearchTextChanged"
                           Background="Transparent"
                           VerticalOptions="Center"/>

                <ActivityIndicator x:Name="LoadingIndicator" 
                                   Grid.Column="2"
                                   IsRunning="False" 
                                   IsVisible="False" 
                                   Color="#6C63FF"
                                   Scale="0.6"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <VerticalStackLayout x:Name="NoResultsLabel" 
                                 IsVisible="False" 
                                 VerticalOptions="Center" 
                                 HorizontalOptions="Center" 
                                 Spacing="15">
                <Label Text="¯\_(ツ)_/¯" FontSize="40" TextColor="#D1D5DB" HorizontalOptions="Center"/>
                <Label Text="Không tìm thấy kết quả nào" 
                       FontSize="16" 
                       TextColor="#6B7280" 
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <CollectionView x:Name="BooksCollection" 
                            ItemsSource="{Binding SearchResults}"
                            SelectionMode="None"
                            Margin="0,10,0,0">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="15"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="20,0">
                            <Border StrokeShape="RoundRectangle 20"
                                    StrokeThickness="0"
                                    BackgroundColor="White"
                                    Padding="0">
                                <Border.Shadow>
                                    <Shadow Brush="Black" Offset="0,5" Radius="15" Opacity="0.05"/>
                                </Border.Shadow>

                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnBookTapped" CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>

                                <Grid ColumnDefinitions="100, *" HeightRequest="140">

                                    <Border Grid.Column="0" 
                                            StrokeShape="RoundRectangle 20,0,0,20" 
                                            StrokeThickness="0"
                                            BackgroundColor="#F3F4F6">
                                        <Image Source="{Binding CoverImageUrl}" 
                                               Aspect="AspectFill"
                                               HorizontalOptions="Fill"
                                               VerticalOptions="Fill"/>
                                    </Border>

                                    <Grid Grid.Column="1" RowDefinitions="Auto, *, Auto" Padding="15,12">

                                        <VerticalStackLayout Grid.Row="0" Spacing="4">
                                            <Label Text="{Binding Title}" 
                                                   FontSize="16" 
                                                   FontAttributes="Bold" 
                                                   TextColor="#1F2937" 
                                                   MaxLines="2" 
                                                   LineBreakMode="TailTruncation"/>

                                            <Label Text="{Binding Authors, Converter={StaticResource AuthorsConverter}}" 
                                                   FontSize="13" 
                                                   TextColor="#6C63FF" 
                                                   FontAttributes="Bold"
                                                   MaxLines="1" 
                                                   LineBreakMode="TailTruncation"/>
                                        </VerticalStackLayout>

                                        <FlexLayout Grid.Row="1" Wrap="Wrap" AlignItems="Center" AlignContent="Center">
                                            <Border BackgroundColor="#F3F4F6" StrokeThickness="0" StrokeShape="RoundRectangle 6" Padding="8,4" Margin="0,0,8,0">
                                                <Label Text="{Binding ManagementCode}" FontSize="11" TextColor="#4B5563" FontAttributes="Bold"/>
                                            </Border>

                                            <Border BackgroundColor="#EFF6FF" StrokeThickness="0" StrokeShape="RoundRectangle 6" Padding="8,4">
                                                <Label Text="{Binding Category}" FontSize="11" TextColor="#2563EB" FontAttributes="Bold"/>
                                            </Border>
                                        </FlexLayout>

                                        <Grid Grid.Row="2" ColumnDefinitions="Auto, *">
                                            <Label Text="{Binding AvailableQuantity, StringFormat='Sẵn có: {0}'}" 
                                                   FontSize="12" 
                                                   FontAttributes="Bold">
                                                <Label.Triggers>
                                                    <DataTrigger TargetType="Label" Binding="{Binding AvailableQuantity, Converter={StaticResource IsZeroOrLessConverter}}" Value="True">
                                                        <Setter Property="TextColor" Value="#EF4444"/>
                                                        <Setter Property="Text" Value="Hết hàng"/>
                                                    </DataTrigger>
                                                    <DataTrigger TargetType="Label" Binding="{Binding AvailableQuantity, Converter={StaticResource IsGreaterThanZeroConverter}}" Value="True">
                                                        <Setter Property="TextColor" Value="#059669"/>
                                                    </DataTrigger>
                                                </Label.Triggers>
                                            </Label>

                                            <Label Grid.Column="1" Text="Chi tiết ➜" FontSize="12" TextColor="#9CA3AF" HorizontalOptions="End"/>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>