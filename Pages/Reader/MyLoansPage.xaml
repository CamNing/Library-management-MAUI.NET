<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Reader.MyLoansPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:book.Converters"
             Title="Sách của tôi"
             BackgroundColor="#F3F4F6"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *">

        <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" Padding="20,15,20,15" BackgroundColor="White">

            <Border StrokeShape="RoundRectangle 20" 
                    HeightRequest="40" 
                    WidthRequest="40" 
                    BackgroundColor="#F3F4F6" 
                    StrokeThickness="0"
                    HorizontalOptions="Start">
                <Label Text="➜" 
                       Rotation="180"
                       FontSize="20" 
                       TextColor="#1F2937" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"/>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnBackClicked"/>
                </Border.GestureRecognizers>
            </Border>

            <Label Grid.Column="1" 
                   Text="Tủ sách của tôi" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   TextColor="#1F2937"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"/>

            <Border Grid.Column="2"
                    StrokeShape="RoundRectangle 20" 
                    HeightRequest="40" 
                    WidthRequest="40" 
                    BackgroundColor="#F3F4F6" 
                    StrokeThickness="0"
                    HorizontalOptions="End">
                <Label Text="⟳" 
                       FontSize="22" 
                       TextColor="#6C63FF" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"/>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnRefreshClicked"/>
                </Border.GestureRecognizers>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <ActivityIndicator x:Name="LoadingIndicator" 
                               IsRunning="False" 
                               IsVisible="False" 
                               Color="#6C63FF"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"/>

            <VerticalStackLayout x:Name="EmptyLabel" 
                                 IsVisible="False" 
                                 VerticalOptions="Center" 
                                 HorizontalOptions="Center" 
                                 Spacing="15">
                <Label Text="📚" FontSize="60" HorizontalOptions="Center" Opacity="0.5"/>
                <Label Text="Bạn chưa mượn cuốn sách nào" 
                       FontSize="16" 
                       TextColor="#6B7280" 
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <CollectionView x:Name="LoansCollection" 
                            ItemsSource="{Binding Loans}"
                            SelectionMode="None"
                            Margin="0,15,0,0">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="15"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="20,0">
                            <Border StrokeShape="RoundRectangle 20"
                                    StrokeThickness="0"
                                    BackgroundColor="White"
                                    Padding="0">
                                <Border.Shadow>
                                    <Shadow Brush="Black" Offset="0,5" Radius="15" Opacity="0.05"/>
                                </Border.Shadow>

                                <Grid ColumnDefinitions="110, *" HeightRequest="160">

                                    <Border Grid.Column="0" 
                                            StrokeShape="RoundRectangle 20,0,0,20" 
                                            StrokeThickness="0"
                                            BackgroundColor="#F3F4F6">
                                        <Image Source="{Binding CoverImageUrl}" 
                                               Aspect="AspectFill"
                                               HorizontalOptions="Fill"
                                               VerticalOptions="Fill"
                                               BackgroundColor="#E5E7EB"/>
                                    </Border>

                                    <Grid Grid.Column="1" RowDefinitions="Auto, *, Auto" Padding="15,12">

                                        <Grid Grid.Row="0" ColumnDefinitions="*, Auto">
                                            <Label Text="{Binding ManagementCode, StringFormat='Mã: {0}'}" 
                                                   FontSize="11" 
                                                   TextColor="#9CA3AF" 
                                                   VerticalOptions="Center"/>

                                            <Border Grid.Column="1" 
                                                    BackgroundColor="#EEF2FF" 
                                                    StrokeThickness="0" 
                                                    StrokeShape="RoundRectangle 6" 
                                                    Padding="8,2">
                                                <Label Text="{Binding Status}" 
                                                       FontSize="10" 
                                                       TextColor="#6C63FF" 
                                                       FontAttributes="Bold"
                                                       TextTransform="Uppercase"/>
                                            </Border>
                                        </Grid>

                                        <VerticalStackLayout Grid.Row="1" Spacing="4" VerticalOptions="Center">
                                            <Label Text="{Binding Title}" 
                                                   FontSize="16" 
                                                   FontAttributes="Bold" 
                                                   TextColor="#1F2937" 
                                                   MaxLines="2" 
                                                   LineBreakMode="TailTruncation"/>

                                            <Label Text="{Binding Authors, Converter={StaticResource AuthorsConverter}}" 
                                                   FontSize="13" 
                                                   TextColor="#6B7280"
                                                   MaxLines="1" 
                                                   LineBreakMode="TailTruncation"/>
                                        </VerticalStackLayout>

                                        <Grid Grid.Row="2" ColumnDefinitions="*, Auto, *" ColumnSpacing="10">

                                            <VerticalStackLayout Grid.Column="0" Spacing="2">
                                                <Label Text="Ngày mượn" FontSize="10" TextColor="#9CA3AF"/>
                                                <Label Text="{Binding BorrowDate, StringFormat='{0:dd/MM/yy}'}" 
                                                       FontSize="13" FontAttributes="Bold" TextColor="#4B5563"/>
                                            </VerticalStackLayout>

                                            <BoxView Grid.Column="1" WidthRequest="1" HeightRequest="25" Color="#F3F4F6" VerticalOptions="Center"/>

                                            <VerticalStackLayout Grid.Column="2" Spacing="2">
                                                <Label Text="Hạn trả" FontSize="10" TextColor="#9CA3AF"/>
                                                <Label Text="{Binding DueDate, StringFormat='{0:dd/MM/yy}'}" 
                                                       FontSize="13" FontAttributes="Bold" TextColor="#EF4444"/>
                                            </VerticalStackLayout>
                                        </Grid>

                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>