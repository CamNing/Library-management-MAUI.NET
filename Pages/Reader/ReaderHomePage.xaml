<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="book.Pages.Reader.ReaderHomePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:book.Models"
             Title="Khám phá"
             BackgroundColor="#F3F4F6">

    <Grid>
        <BoxView IsHitTestVisible="False">
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#F3F4F6" Offset="0.0" />
                    <GradientStop Color="#EDE9FE" Offset="1.0" />
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>

        <Grid RowDefinitions="Auto, *">

            <VerticalStackLayout Grid.Row="0" Spacing="15" Padding="20,10,20,10" BackgroundColor="Transparent">

                <Grid ColumnDefinitions="*, Auto">
                    <VerticalStackLayout Spacing="2">
                        <Label Text="Xin chào," FontSize="14" TextColor="#6B7280" />
                        <Label Text="Bạn muốn đọc gì hôm nay?" FontSize="20" FontAttributes="Bold" TextColor="#1F2937" />
                    </VerticalStackLayout>

                    <Border Grid.Column="1" 
                            StrokeShape="RoundRectangle 12" 
                            BackgroundColor="White" 
                            StrokeThickness="0"
                            HeightRequest="40" 
                            WidthRequest="40">
                        <Border.Shadow>
                            <Shadow Brush="#000000" Opacity="0.05" Offset="0,4" Radius="10" />
                        </Border.Shadow>
                        <Label Text="⏻" 
                       FontSize="20" 
                       TextColor="#EF4444" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"
                       FontAttributes="Bold"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnLogoutClicked"/>
                        </Border.GestureRecognizers>
                    </Border>
                </Grid>

                <Border StrokeShape="RoundRectangle 15" 
                        BackgroundColor="White" 
                        StrokeThickness="0"
                        HeightRequest="50">
                    <Border.Shadow>
                        <Shadow Brush="#000000" Opacity="0.05" Offset="0,4" Radius="10" />
                    </Border.Shadow>
                    <Grid ColumnDefinitions="50, *" Padding="0">
                        <Label Text="🔍" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#9CA3AF"/>
                        <Label Grid.Column="1" 
                               Text="Tìm kiếm tên sách, tác giả..." 
                               TextColor="#9CA3AF" 
                               VerticalOptions="Center"
                               FontSize="14"/>
                    </Grid>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnSearchClicked"/>
                    </Border.GestureRecognizers>
                </Border>

                <Border StrokeShape="RoundRectangle 15"
                        BackgroundColor="#6C63FF"
                        StrokeThickness="0"
                        HeightRequest="60">
                    <Border.Shadow>
                        <Shadow Brush="#6C63FF" Opacity="0.3" Offset="0,5" Radius="10"/>
                    </Border.Shadow>
                    <Grid ColumnDefinitions="Auto, *, Auto" Padding="20,0">
                        <Label Text="📚" FontSize="24" VerticalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Margin="15,0,0,0">
                            <Label Text="Sách đang mượn" TextColor="White" FontAttributes="Bold" FontSize="16"/>
                            <Label Text="Kiểm tra hạn trả và trạng thái" TextColor="#E0E7FF" FontSize="12"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="2" Text="→" TextColor="White" FontSize="24" VerticalOptions="Center"/>
                    </Grid>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnMyLoansClicked"/>
                    </Border.GestureRecognizers>
                </Border>
            </VerticalStackLayout>

            <ScrollView Grid.Row="1" VerticalScrollBarVisibility="Never">
                <VerticalStackLayout Spacing="25" Padding="0,10,0,80">

                    <VerticalStackLayout Spacing="10">
                        <Label Text="✨ Mới Cập Nhật" 
                               FontSize="18" 
                               FontAttributes="Bold" 
                               TextColor="#1F2937" 
                               Margin="20,0"/>

                        <CollectionView ItemsSource="{Binding NewBooks}" HeightRequest="320">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Horizontal" ItemSpacing="15"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.Header>
                                <BoxView WidthRequest="5" Color="Transparent"/>
                            </CollectionView.Header>
                            <CollectionView.Footer>
                                <BoxView WidthRequest="20" Color="Transparent"/>
                            </CollectionView.Footer>

                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border WidthRequest="160" 
                                            HeightRequest="290"
                                            StrokeShape="RoundRectangle 15"
                                            BackgroundColor="White"
                                            StrokeThickness="0"
                                            Margin="0,5,0,15">
                                        <Border.Shadow>
                                            <Shadow Brush="#000000" Opacity="0.1" Offset="0,5" Radius="10"/>
                                        </Border.Shadow>

                                        <Grid RowDefinitions="*, Auto" Padding="0">
                                            <Image Source="{Binding CoverImageUrl}" 
                                                   Aspect="AspectFill"
                                                   VerticalOptions="FillAndExpand"
                                                   BackgroundColor="#F3F4F6"/>

                                            <Border BackgroundColor="#CCFFFFFF" 
                                                    StrokeShape="RoundRectangle 8"
                                                    HorizontalOptions="End"
                                                    VerticalOptions="Start"
                                                    Margin="10"
                                                    Padding="8,4"
                                                    StrokeThickness="0">
                                                <Label Text="{Binding AvailableQuantity, StringFormat='Còn: {0}'}"
                                                       FontSize="10"
                                                       FontAttributes="Bold"
                                                       TextColor="#6C63FF"/>
                                            </Border>

                                            <VerticalStackLayout Grid.Row="1" Padding="12" Spacing="4" BackgroundColor="White">
                                                <Label Text="{Binding Title, Converter={StaticResource TitleTruncateConverter}}" 
                                                       FontSize="14" 
                                                       FontAttributes="Bold"
                                                       LineBreakMode="TailTruncation"
                                                       TextColor="#1F2937"/>

                                                <Label Text="{Binding Authors, Converter={StaticResource AuthorsConverter}}" 
                                                       FontSize="11"
                                                       TextColor="#9CA3AF"
                                                       LineBreakMode="TailTruncation"/>

                                                <Button Text="Chi tiết" 
                                                        Clicked="OnViewDetailClicked"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="#F3F4F6"
                                                        TextColor="#6C63FF"
                                                        FontSize="11"
                                                        FontAttributes="Bold"
                                                        Padding="0"
                                                        HeightRequest="30"
                                                        CornerRadius="8"
                                                        Margin="0,5,0,0"/>
                                            </VerticalStackLayout>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="10">
                        <Grid ColumnDefinitions="*, Auto" Margin="20,0">
                            <Label Text="🔥 Phổ Biến Nhất" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   TextColor="#1F2937"/>
                        </Grid>

                        <CollectionView ItemsSource="{Binding MostAccessedBooks}" HeightRequest="140">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Horizontal" ItemSpacing="15"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.Header>
                                <BoxView WidthRequest="5" Color="Transparent"/>
                            </CollectionView.Header>
                            <CollectionView.Footer>
                                <BoxView WidthRequest="20" Color="Transparent"/>
                            </CollectionView.Footer>

                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border WidthRequest="280" 
                                            HeightRequest="120"
                                            StrokeShape="RoundRectangle 15"
                                            BackgroundColor="White"
                                            StrokeThickness="0"
                                            Margin="0,5,0,10">
                                        <Border.Shadow>
                                            <Shadow Brush="#000000" Opacity="0.08" Offset="0,4" Radius="8"/>
                                        </Border.Shadow>

                                        <Grid ColumnDefinitions="90, *" Padding="0">
                                            <Image Grid.Column="0" 
                                                   Source="{Binding CoverImageUrl}" 
                                                   Aspect="AspectFill"
                                                   BackgroundColor="#F3F4F6"/>

                                            <VerticalStackLayout Grid.Column="1" Padding="12" Spacing="5" VerticalOptions="Center">
                                                <Label Text="{Binding Title}" 
                                                       FontSize="14" 
                                                       FontAttributes="Bold"
                                                       MaxLines="2"
                                                       LineBreakMode="TailTruncation"
                                                       TextColor="#1F2937"/>

                                                <Label Text="{Binding Authors, Converter={StaticResource AuthorsConverter}}" 
                                                       FontSize="12"
                                                       TextColor="#9CA3AF"
                                                       LineBreakMode="TailTruncation"/>

                                                <Grid ColumnDefinitions="*, Auto" Margin="0,5,0,0">
                                                    <Label Text="{Binding ViewCount, StringFormat='👁 {0} lượt xem'}" 
                                                           FontSize="11" 
                                                           TextColor="#6C63FF" 
                                                           VerticalOptions="Center"/>

                                                    <Button Grid.Column="1"
                                                            Text="Xem" 
                                                            Clicked="OnViewDetailClicked"
                                                            CommandParameter="{Binding .}"
                                                            BackgroundColor="#F3F4F6"
                                                            TextColor="#1F2937"
                                                            FontSize="11"
                                                            FontAttributes="Bold"
                                                            Padding="15,0"
                                                            HeightRequest="30"
                                                            CornerRadius="15"/>
                                                </Grid>
                                            </VerticalStackLayout>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </ScrollView>

            <Grid Grid.RowSpan="2" 
                  BackgroundColor="#80FFFFFF" 
                  IsVisible="{Binding IsRunning, Source={x:Reference LoadingIndicator}}"
                  ZIndex="99">
                <ActivityIndicator x:Name="LoadingIndicator" 
                                  IsRunning="False" 
                                  IsVisible="False" 
                                  Color="#6C63FF"
                                  WidthRequest="50"
                                  HeightRequest="50"
                                  VerticalOptions="Center"
                                  HorizontalOptions="Center"/>
            </Grid>
        </Grid>

        <Button Text="💬"
                Clicked="OnChatBubbleClicked"
                BackgroundColor="#6C63FF"
                TextColor="White"
                FontSize="24"
                CornerRadius="30"
                WidthRequest="60"
                HeightRequest="60"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="25"
                Padding="0"
                ZIndex="100">
            <Button.Shadow>
                <Shadow Brush="#6C63FF" Offset="0,4" Radius="10" Opacity="0.5"/>
            </Button.Shadow>
        </Button>
    </Grid>
</ContentPage>